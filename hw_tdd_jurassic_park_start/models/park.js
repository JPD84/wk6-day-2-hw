const Park = function (name, ticketPrice, collectionOfDinosaurs){
  this.parkName = parkName;
  this.ticketPrice = ticketPrice;
  this.collectionOfDinosaurs = collectionOfDinosaurs;
}

Park.prototype.parkName = function (parkName) {
  this.parkName(name);
};

Park.prototype.ticketPrice = function (ticketPrice) {
  this.ticketPrice();
};

Park.prototype.addDinosaur = function (dinosaur) {
this.species.dinosaur.push(dinosaur);
};


Park.prototype.removeDinosaur = function (collectionOfDinosaurs) {
this.species.pop(dinosaur)
};

Park.prototype. = function () {
getMax = object => {
return Object.keys(dinosaur).filter(x => {
return object[x] == Math.max.apply(null,
Object.values(dinosaur));

};
};

module.exports = Park;
